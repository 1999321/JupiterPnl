version: "1.0"

services:
  rust-app:
    build: .  # 使用当前目录的 Dockerfile 构建镜像
    ports:
      - "80:80"  # 暴露主机 80 端口到容器 80 端口
    restart: unless-stopped  # 容器意外退出时自动重启
    environment:
      - RUST_LOG=info  # 可选：设置Rust日志级别